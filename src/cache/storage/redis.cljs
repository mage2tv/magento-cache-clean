(ns cache.storage.redis)

;; php -r 'echo json_encode((require "app/etc/env.php")["cache"]["frontend"] ?? []);'

(defn tag->ids [tag]
  [])

(defn delete [id]
  )

(defn clean-tag [tag]
  )

(defn clean-all []
  )
